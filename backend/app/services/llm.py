"""
LLM-сервис: GigaChat.

GigaChat API совместим с OpenAI-форматом (chat completions).
Авторизация: OAuth2 через RqUID + Base64(client_id:client_secret).
"""

import asyncio
import base64
import html
import logging
import uuid
from datetime import UTC, datetime

import httpx

from app.core.config import settings

logger = logging.getLogger(__name__)

_gigachat_token: str | None = None
_gigachat_token_expires_at: datetime | None = None
_gigachat_lock = asyncio.Lock()

GIGACHAT_AUTH_URL = "https://ngw.devices.sberbank.ru:9443/api/v2/oauth"
GIGACHAT_API_URL = "https://gigachat.devices.sberbank.ru/api/v1"


async def _get_gigachat_token() -> str:
    global _gigachat_token, _gigachat_token_expires_at

    async with _gigachat_lock:
        if (
            _gigachat_token
            and _gigachat_token_expires_at
            and _gigachat_token_expires_at > datetime.now(UTC)
        ):
            return _gigachat_token

        # verify=False: GigaChat использует CA «МинЦифры России», не входящий
        # в стандартный доверенный список. В продакшне можно добавить CA-cert
        # через httpx.AsyncClient(verify="/path/to/mintsifry_ca.pem").
        async with httpx.AsyncClient(verify=False) as client:
            resp = await client.post(
                GIGACHAT_AUTH_URL,
                headers={
                    "Authorization": f"Basic {settings.GIGACHAT_AUTH_KEY}",
                    "RqUID": str(uuid.uuid4()),
                    "Content-Type": "application/x-www-form-urlencoded",
                },
                data={"scope": "GIGACHAT_API_PERS"},
                timeout=10,
            )
            if not resp.is_success:
                logger.error("GigaChat auth failed %s: %s", resp.status_code, resp.text)
                resp.raise_for_status()
            data = resp.json()

        _gigachat_token = data["access_token"]
        _gigachat_token_expires_at = datetime.fromtimestamp(data["expires_at"] / 1000, tz=UTC)
        return _gigachat_token


async def _call_gigachat(system_prompt: str, user_prompt: str) -> str:
    token = await _get_gigachat_token()
    async with httpx.AsyncClient(verify=False) as client:
        resp = await client.post(
            f"{GIGACHAT_API_URL}/chat/completions",
            headers={"Authorization": f"Bearer {token}"},
            json={
                "model": "GigaChat",
                "messages": [
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": user_prompt},
                ],
                "temperature": 0.2,
                "max_tokens": 4096,
            },
            timeout=60,
        )
        resp.raise_for_status()
        return resp.json()["choices"][0]["message"]["content"]



BASE_RULES = """Правила:
- Официально-деловой стиль, без эмоций и просторечий
- Ссылайся на конкретные статьи закона (не выдумывай несуществующие)
- Не придумывай факты — используй только данные пользователя
- Квадратные скобки [...] используй ТОЛЬКО для данных, которых нет в форме и которые пользователь впишет вручную
- Все данные из формы (ФИО, адрес, даты, суммы, названия) вставляй напрямую — БЕЗ квадратных скобок
- Даты из формы в формате ГГГГ-ММ-ДД переводи в русский формат: «1 мая 2026 года»
- Для строки подписи в конце используй: «_________________ / _________________»
- Верни ТОЛЬКО текст документа, без пояснений и комментариев"""

SYSTEM_PROMPTS: dict[str, str] = {
    "shop": f"""Ты — юрист по защите прав потребителей. Составь претензию в магазин.

Структура документа:
1. Шапка: «Руководителю [название магазина], [адрес]» / «От: [ФИО], [адрес], [телефон], [email]»
2. Заголовок: ПРЕТЕНЗИЯ
3. Описание: когда куплен товар, что за товар, стоимость, что произошло
4. Если в данных есть appeal_date и store_response — добавь абзац: «[дата обращения] я обратился(ась) в магазин с требованием [demand]. Магазин [ответил: указать суть ответа / не дал ответа / отказал, сославшись на: указать основание].» Укажи, что отказ является незаконным, со ссылкой на норму.
5. Правовое обоснование: ссылки на ЗоЗПП ст. 18 (некачественный), ст. 25 (14 дней), ст. 23 (неустойка 1%/день), ГК РФ ст. 475
6. Расчёт неустойки: product_price × 1% × количество_дней (от appeal_date до даты составления документа). Если appeal_date не указана — не делай расчёт, напиши только формулу со скобками.
7. Требование: конкретная сумма, срок исполнения 10 дней (ст. 22 ЗоЗПП)
8. Предупреждение: при повторном отказе — иск в суд + штраф 50% (ст. 13 ЗоЗПП) + моральный вред
9. Дата и подпись

{BASE_RULES}""",

    "marketplace": f"""Ты — юрист по защите прав потребителей. Составь претензию на маркетплейс (Wildberries, Ozon и т.д.).

Структура документа:
1. Шапка: «Генеральному директору [название маркетплейса]» + юридический адрес / «От: [ФИО], [адрес], [телефон], [email]»
2. Заголовок: ПРЕТЕНЗИЯ
3. Описание: номер заказа, товар, дата, что произошло (незаконный штраф / платный возврат / потеря)
4. Правовое обоснование: ЗоЗПП ст. 26.1 (дистанционная торговля), Постановление Правительства №2463, ЗоЗПП ст. 18
5. Указание на незаконность действий маркетплейса со ссылками на нормы
6. Требование: конкретная сумма к возврату, срок 10 дней
7. При игнорировании — жалоба в Роспотребнадзор и иск в суд
8. Дата и подпись

{BASE_RULES}""",

    "bank": f"""Ты — юрист по банковскому праву и защите прав потребителей. Составь претензию/заявление в банк.

Структура документа:
1. Шапка: «Руководителю [название банка]» / «От: [ФИО], [адрес], [телефон], [email]»
2. Заголовок: ЗАЯВЛЕНИЕ-ПРЕТЕНЗИЯ (или ПРЕТЕНЗИЯ)
3. Описание: номер договора/счёта, дата нарушения, что произошло
4. Правовое обоснование:
   - При страховке: ФЗ «О потребительском кредите» ст. 7, Указание ЦБ №3854-У (период охлаждения 30 дней), ЗоЗПП ст. 16
   - При блокировке: ФЗ-115 ст. 7-8, право на уведомление об основаниях
   - При комиссии: ЗоЗПП ст. 16, ГК РФ ст. 168
5. Конкретное требование с суммой и сроком (30 дней)
6. При отказе — жалоба в ЦБ (cbr.ru) и Роспотребнадзор + иск в суд
7. Дата и подпись

{BASE_RULES}""",

    "employer": f"""Ты — юрист по трудовому праву. Составь претензию работодателю о выплате задолженности.

Структура документа:
1. Шапка: «Руководителю [название организации, ИНН]» / «От: [ФИО], должность, [адрес], [телефон], [email]»
2. Заголовок: ПРЕТЕНЗИЯ (о выплате задержанной заработной платы / расчёта)
3. Описание: должность, период работы (если указан hire_date), за какой период не выплачено (debt_period), сумма долга (debt_amount)
4. Если указан last_payment_date — рассчитай количество дней задержки от этой даты до даты составления документа и подставь в расчёт.
5. Правовое обоснование: ТК РФ ст. 136 (сроки выплаты), ст. 140 (расчёт при увольнении), ст. 236 (компенсация 1/150 ключевой ставки ЦБ), ст. 142 (право приостановить работу)
6. Расчёт: debt_amount × 1/150 × ключевая_ставка_ЦБ_РФ% × дней_задержки = компенсация. Используй актуальную ключевую ставку ЦБ (если не знаешь точно — используй 21%). Итого к выплате: долг + компенсация.
7. Если указаны additional_desc — добавь отдельный абзац «Дополнительные обстоятельства» с этой информацией.
8. Требование: выплатить долг + компенсацию в течение 3 рабочих дней
9. При неисполнении — обращение в ГИТ (онлайнинспекция.рф) и прокуратуру, иск в суд
10. Дата и подпись

{BASE_RULES}""",

    "insurance": f"""Ты — юрист по страховому праву. Составь досудебную претензию в страховую компанию.

Структура документа:
1. Шапка: «Генеральному директору [страховая компания]» / «От: [ФИО], [адрес], [телефон], [email]»
2. Заголовок: ДОСУДЕБНАЯ ПРЕТЕНЗИЯ
3. Описание: тип полиса (ОСАГО/КАСКО), номер полиса, дата страхового события, обстоятельства (incident_desc)
4. Правовое обоснование:
   - ОСАГО: ФЗ «Об ОСАГО» ст. 12 (сроки), ст. 16.1 (досудебная претензия обязательна), ст. 12 п.21 (неустойка 1%/день)
   - КАСКО: ГК РФ ст. 929, 943; ЗоЗПП ст. 28 (неустойка за просрочку)
5. Расчёт (используй реальные числа из формы):
   - Если указаны actual_damage и paid_amount: недоплата = actual_damage − paid_amount
   - Если указаны overdue_days: неустойка = недоплата × 1% × overdue_days
   - Итого к выплате = недоплата + неустойка
   - Если actual_damage не указан (refusal) — требуй полную выплату по результатам независимой экспертизы
6. Требование: выплатить конкретную сумму в течение 10 рабочих дней
7. При отказе — жалоба в РСА и ЦБ + иск в суд (штраф 50% от взысканной суммы, моральный вред)
8. Дата и подпись

{BASE_RULES}""",

    "utility": f"""Ты — юрист по жилищному праву. Составь претензию в управляющую компанию или ТСЖ.

Структура документа:
1. Шапка: «Руководителю [название УК/ТСЖ]» / «От: [ФИО], [адрес квартиры], [телефон], [email]»
2. Заголовок: ПРЕТЕНЗИЯ (о перерасчёте / устранении нарушений)
3. Описание: адрес дома, характер нарушения, период, суть проблемы
4. Правовое обоснование: ЖК РФ ст. 161, Постановление Правительства №491 (содержание общего имущества), №354 (коммунальные услуги и перерасчёт), КоАП ст. 7.22 (штраф для УК)
5. Расчёт перерасчёта (если применимо) или перечень нарушений
6. Требование: произвести перерасчёт / устранить нарушения в течение 30 дней
7. При неисполнении — жалоба в ГЖИ, прокуратуру, иск в суд
8. Дата и подпись

{BASE_RULES}""",

    "airline": f"""Ты — юрист по транспортному праву. Составь претензию авиакомпании.

Структура документа:
1. Шапка: «Генеральному директору [авиакомпания]» + юридический адрес / «От: [ФИО], [адрес], [телефон], [email]»
2. Заголовок: ПРЕТЕНЗИЯ
3. Описание: рейс, маршрут, дата, что произошло (используй problem_desc из формы)
4. Правовое обоснование:
   - Задержка: Воздушный кодекс РФ ст. 120 (25 руб./час + штраф 50% стоимости билета), ФАП №82 (питание, гостиница)
   - Отмена / возврат: ВК РФ ст. 107, ст. 108; ЗоЗПП ст. 28
   - Багаж: ВК РФ ст. 119
5. Расчёт (используй реальные числа из формы):
   - Если delay_hours > 0: компенсация за задержку = 25 × delay_hours, штраф = 50% × ticket_price
   - Доп. расходы = extra_expenses (если указано)
   - Итого к выплате = компенсация + штраф + extra_expenses − received_compensation
   - Если received_compensation = 0 или не указано — не вычитай
6. Требование: выплатить конкретную сумму в течение 30 дней
7. При отказе — жалоба в Росавиацию + иск в суд (штраф 50% по ЗоЗПП)
8. Дата и подпись

{BASE_RULES}""",
}

_DEFAULT_SYSTEM_PROMPT = f"""Ты — опытный юрист. Составь официальную претензию или жалобу.
Используй официально-деловой стиль. Ссылайся только на реальные нормы российского права.
{BASE_RULES}"""


_MONTHS_RU = ["января","февраля","марта","апреля","мая","июня",
              "июля","августа","сентября","октября","ноября","декабря"]


def _build_user_prompt(situation_id: str, form_data: dict) -> str:
    now = datetime.now(UTC)
    today = f"{now.day} {_MONTHS_RU[now.month - 1]} {now.year} года"
    lines = [f"Ситуация: {situation_id}", f"Дата составления документа: {today}", "", "Данные пользователя:"]
    for k, v in form_data.items():
        if v:
            lines.append(f"- {k}: {v}")
    return "\n".join(lines)


async def fill_template(situation_id: str, form_data: dict) -> str:
    system_prompt = SYSTEM_PROMPTS.get(situation_id, _DEFAULT_SYSTEM_PROMPT)
    user_prompt = _build_user_prompt(situation_id, form_data)

    if not settings.GIGACHAT_AUTH_KEY:
        raise RuntimeError("GigaChat not configured — set GIGACHAT_AUTH_KEY")

    return await _call_gigachat(system_prompt, user_prompt)
